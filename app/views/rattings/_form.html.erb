<%= form_for(@ratting) do |f| %>
  <% if @ratting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ratting.errors.count, "error") %> prohibited this ratting from being saved:</h2>

      <ul>
      <% @ratting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :ratting %><br>
    <%= f.number_field :ratting %>
  </div>
  <div class="field">
    <%= f.label :shop_id %><br>
    <%= f.number_field :shop_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
